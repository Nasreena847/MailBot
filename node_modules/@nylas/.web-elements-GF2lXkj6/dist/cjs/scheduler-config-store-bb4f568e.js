'use strict';

const index = require('./index-e31dc92c.js');

function CreateNylasSchedulerConfigStore(defaultState = {}) {
    const defaultSchedulerConfigState = {
        selectedConfiguration: {},
        configurations: [],
        listConfigurationsNextCursor: null,
        calendars: [],
        currentUser: null,
        action: null,
        additionalParticipants: [],
        hideEditorTabs: [],
        requiresSlug: false,
        ...defaultState,
    };
    const store = index.createStore(defaultSchedulerConfigState);
    function resetSelectedConfiguration() {
        store.set('selectedConfiguration', { ...defaultState['selectedConfiguration'] });
    }
    store.onChange('action', async (action) => {
        if (!action) {
            resetSelectedConfiguration();
        }
    });
    return {
        ...store,
        resetSelectedConfiguration,
    };
}

exports.CreateNylasSchedulerConfigStore = CreateNylasSchedulerConfigStore;

//# sourceMappingURL=scheduler-config-store-bb4f568e.js.map